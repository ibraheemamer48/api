const express = require('express')
const router = express.Router()

// Getting all
router.get('/', (req, res) => {
    
})
// Getting one
router.get('/:id', (req, res) => {

})

// Creating one
router.post('', (req, res) => {
    
})

// Updating One
// Deleting one

module.exports = router